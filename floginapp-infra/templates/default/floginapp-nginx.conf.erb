server {
    listen <%= node['floginapp-infra']['server_port'] %>;
    server_name <%= node['floginapp-infra']['server_name'] %>;

    location / {
        include proxy_params;
        proxy_pass http://unix:<%= node['floginapp-infra']['gunicorn_socket'] %>;
    }
}
